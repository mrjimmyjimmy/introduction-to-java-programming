package observer.wrong;

/**
 * @author jimmy xu
 * @date 2020/9/17 10:51
 */
public class WeatherDataWrong {

    private double temperature;
    private double humidity;
    private double pressure;

    public void measurementsChanged() {
        /*
        temp = getTmp();
        humidity = getHum();
        pressure = getPre();

        board1.update(temp, hum, pre);
        board2.update(temp, hum, pre);
        board3.update(temp, hum, pre);
         */
    }

}
